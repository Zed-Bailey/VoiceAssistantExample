# VoiceAssistantExample

working example of a simple voice assistant that can respond to a weather request

## Running the example
clone the repo and `cd` into it, then execute `./gradlew run --args="configuration.properties`
